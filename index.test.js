const test = require('tape')
const index = require('./index')


test('sal1', (t) => {
    t.assert(index.calcularSalario(salario_bruto) === 100,
    "salario")
    t.end()
    return salario_liquido
})



